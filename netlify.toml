[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  # Avoid bundling issues with json-server (dynamic requires, fs usage, etc.)
  node_bundler = "zisi"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200



